# GarakBoard — Docker Compose stack
# Usage: docker compose --env-file docker/.env -f docker/docker-compose.yml up
# Copy docker/.env.docker to docker/.env and fill in your values before first run.

services:

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: garakboard
      POSTGRES_USER: garakboard
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in docker/.env (copy from docker/.env.docker)}
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U garakboard -d garakboard"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    restart: unless-stopped
    environment:
      # Use Docker service names — not localhost — so containers can reach each other
      DATABASE_URL: postgresql://garakboard:${POSTGRES_PASSWORD}@postgres:5432/garakboard
      REDIS_URL: redis://redis:6379/0
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in docker/.env}
      API_HOST: "0.0.0.0"
      API_PORT: "8000"
      API_BASE_URL: http://api:8000
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile.worker
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://garakboard:${POSTGRES_PASSWORD}@postgres:5432/garakboard
      REDIS_URL: redis://redis:6379/0
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in docker/.env}
      OPENROUTER_RPM_LIMIT: ${OPENROUTER_RPM_LIMIT:-60}
      GARAK_PARALLEL_ATTEMPTS: ${GARAK_PARALLEL_ATTEMPTS:-10}
      GARAK_SOFT_PROBE_PROMPT_CAP: ${GARAK_SOFT_PROBE_PROMPT_CAP:-50}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  beat:
    build:
      context: ..
      dockerfile: docker/Dockerfile.worker
    restart: unless-stopped
    command: >
      celery -A garakboard.worker.celery_app:celery_app beat
      --loglevel=info
      --scheduler celery.beat:PersistentScheduler
    environment:
      DATABASE_URL: postgresql://garakboard:${POSTGRES_PASSWORD}@postgres:5432/garakboard
      REDIS_URL: redis://redis:6379/0
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in docker/.env}
    depends_on:
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    restart: unless-stopped
    command: ["python", "-m", "garakboard.frontend.gradio_app"]
    environment:
      DATABASE_URL: postgresql://garakboard:${POSTGRES_PASSWORD}@postgres:5432/garakboard
      REDIS_URL: redis://redis:6379/0
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in docker/.env}
      # Points to the API service name on the Docker network — not localhost
      API_BASE_URL: http://api:8000
      GRADIO_SERVER_PORT: "7860"
    ports:
      - "7860:7860"
    depends_on:
      api:
        condition: service_healthy

  flower:
    build:
      context: ..
      dockerfile: docker/Dockerfile.worker
    restart: unless-stopped
    command: >
      celery -A garakboard.worker.celery_app:celery_app flower
      --port=5555
    environment:
      DATABASE_URL: postgresql://garakboard:${POSTGRES_PASSWORD}@postgres:5432/garakboard
      REDIS_URL: redis://redis:6379/0
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in docker/.env}
    ports:
      - "5555:5555"
    depends_on:
      redis:
        condition: service_healthy

volumes:
  pgdata:
