# Copy to docker/.env and fill in values
# Used by: docker compose --env-file docker/.env -f docker/docker-compose.yml up

# Required
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Optional overrides (defaults shown)
POSTGRES_PASSWORD=garakboard
OPENROUTER_RPM_LIMIT=14
GARAK_PARALLEL_ATTEMPTS=1